apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-host
spec:
  rules:
  - host: shyam-pagadi.com
    http:
      paths:
      - pathType: Prefix
        path: /
        backend:
          service:
            name: httpd
            port:
              number: 80
